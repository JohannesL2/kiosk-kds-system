version: '3.8'
services:
  kiosk-app:
    build: .
    ports:
      - "5173:5173"
      - "4000:4000"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development